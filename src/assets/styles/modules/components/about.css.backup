/* About Component Styles */

.about-bg {
  background: var(--s1);
}

/* Mission section */
.about-mission {
  text-align: center;
  max-width: 800px;
  margin: 0 auto;
}

.mission-content h3 {
  margin-bottom: 16px;
  font-size: 1.5rem;
  color: var(--t);
}

.mission-text {
  font-size: 1.1rem;
  line-height: 1.6;
  color: var(--t2);
  margin: 0;
  padding-bottom: 24px;
}

/* Story grid */
.about-grid {
  gap: 40px;
  align-items: start;
}

.story-content,
.approach-content {
  background: var(--grad-card);
  border: 1px solid var(--bd);
  border-radius: 12px;
  padding: 28px;
}

.story-content h3,
.approach-content h3 {
  margin: 0 0 16px;
  font-size: 1.3rem;
  color: var(--t);
}

.story-content p,
.approach-content p {
  margin: 0;
  line-height: 1.6;
  color: var(--t2);
}

/* Values grid */
.values-grid {
  gap: 24px;
}

.value-card {
  background: var(--grad-card);
  border: 1px solid var(--bd);
  border-radius: 12px;
  overflow: hidden;
  position: relative;
  text-align: center;
  padding: 32px 24px;
}

.value-icon {
  width: 60px;
  height: 60px;
  background: linear-gradient(135deg, var(--mg) 0%, var(--g) 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 20px;
  font-size: 1.5rem;
  color: white;
}

.value-card h4 {
  margin: 0 0 12px;
  font-size: 1.2rem;
  color: var(--t);
}

.value-card p {
  margin: 0;
  line-height: 1.5;
  color: var(--t2);
}

/* Team grid */
.team-grid {
  gap: 24px;
}

.team-card {
  background: var(--grad-card);
  border: 1px solid var(--bd);
  border-radius: 12px;
  overflow: hidden;
  position: relative;
  text-align: center;
  padding: 28px 24px;
}

.team-card h4 {
  margin: 0 0 8px;
  font-size: 1.3rem;
  color: var(--t);
}

.team-role {
  font-size: 0.9rem;
  color: var(--mg);
  font-weight: 600;
  margin-bottom: 16px;
  text-transform: uppercase;
  letter-spacing: 0.02em;
}

.team-card p {
  margin: 0;
  line-height: 1.5;
  color: var(--t2);
}

.team-join {
  margin-top: 40px;
}

.text-link {
  color: var(--mg);
  text-decoration: none;
  transition: var(--tr);
}

@media (hover: hover) {
  .text-link:hover {
    color: var(--g);
    text-decoration: underline;
  }
}

/* Responsive adjustments */
@media (max-width: 1024px) {
  .values-grid {
    grid-template-columns: 1fr;
    gap: 20px;
  }
}

@media (max-width: 768px) {
  .about-grid {
    grid-template-columns: 1fr;
    gap: 24px;
  }

  .team-grid {
    grid-template-columns: 1fr;
    gap: 20px;
  }

  .story-content,
  .approach-content {
    padding: 20px;
  }

  .value-card,
  .team-card {
    padding: 24px 20px;
  }

  .mission-text {
    font-size: 1rem;
  }
}

@media (max-width: 480px) {
  .value-icon {
    width: 50px;
    height: 50px;
    font-size: 1.3rem;
    margin-bottom: 16px;
  }

  .story-content,
  .approach-content {
    padding: 16px;
  }
}

